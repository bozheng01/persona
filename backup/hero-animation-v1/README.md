# Hero animation — backup v1

首页 Hero 动画的备份（2026-09 版本）。包含：

- `index.html` — 当时完整的页面（含内联 Hero 场景 SVG）
- `site.css` — 当时完整的样式（含云漂移 / 角色浮动 / 樱花动画）
- `hero-scene.svg` — 仅 Hero 场景那段内联 SVG，方便快速取用

恢复方式：把对应文件覆盖回项目根目录 / `assets/` 即可。
git 标签 `hero-animation-v1` 也指向这一版。
